const person = {
    name: "Surya",
    occupation: "Product Designer",
    city: "Delhi"
}

for (let i = 0; i < 5; i++) {
    console.log(`Hi, I'm ${person.name}, a ${person.occupation} based out of ${person.city}.`)    
}
